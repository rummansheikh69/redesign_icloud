# Python-Backend

Flask-Anwendung mit SQLAlchemy (SQLite) und Flask-SocketIO für Echtzeit-Updates.

## Starten

```powershell
cd ..\..  # Projekt-Root
pip install -r backend\requirements.txt
python run.py
```

Oder direkt:

```powershell
python backend\app.py
```

Standard-Port: `4000`.
